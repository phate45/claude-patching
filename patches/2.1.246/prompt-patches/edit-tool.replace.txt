Exact string replacement.${DbY()} Preserve exact indentation from output (ignore line number prefix). Keep old_string minimal (1-3 lines) - just enough to be unique. Fails if old_string not unique - add context or use replace_all. Prefer editing over creating new files.