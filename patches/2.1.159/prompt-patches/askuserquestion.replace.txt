Ask user questions to gather preferences, clarify instructions, or get implementation decisions. Users can always select "Other" for custom input. Use multiSelect: true for multiple selections.
- If you recommend a specific option, make that the first option in the list and add "(Recommended)" at the end of the label

Plan mode note: In plan mode, use this tool to clarify requirements or choose between approaches BEFORE finalizing your plan. Do NOT use this tool to ask "Is my plan ready?" or "Should I proceed?" - use ${oG} for plan approval. IMPORTANT: Do not reference "the plan" in your questions (e.g., "Do you have feedback about the plan?", "Does the plan look good?") because the user cannot see the plan in the UI until you call ${oG}. If you need plan approval, use ${oG} instead.
