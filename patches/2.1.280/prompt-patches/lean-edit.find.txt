Performs exact string replacement in a file.
${__NFLAG__?`
- If the file is outside the working directory, you must ${__READ__} it in this conversation before editing, or the call will fail.`:`
- You must ${__READ__} the file in this conversation before editing, or the call will fail.`}
- \`old_string\` must match the file exactly, including indentation, and be unique — the edit fails otherwise. Strip the Read line prefix (${__SVAR__}) before matching.
- \`replace_all: true\` replaces every occurrence instead.