Launch agents for complex, multi-step tasks. Specify subagent_type parameter.

Available agent types are listed in <system-reminder> messages in the conversation.${__PVAR__}

Delegate broad, multi-file exploration to an Explore agent and keep the conclusion, not the file dumps. For a known file, symbol, or string, search directly. Once you've delegated a search, don't also run it yourself — wait for the result.
